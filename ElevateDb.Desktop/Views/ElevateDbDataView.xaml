<UserControl
    x:Class="ElevateDb.Desktop.Views.ElevateDbDataView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ElevateDb.Desktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ElevateDb.Desktop.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:ElevateDbDataViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Style="{StaticResource SeparatorRowStyle}" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Style="{StaticResource SeparatorColumnStyle}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Row="0"
            Grid.RowSpan="3"
            Grid.Column="0"
            Orientation="Vertical">
            <TextBlock Text="Tables:" />
            <ListBox
                Height="200"
                ItemsSource="{Binding DbTableNames, UpdateSourceTrigger=PropertyChanged}"
                SelectedItem="{Binding SelectedTable, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Command="{Binding LoadDbTablesCommand}"
                Content="Read Config Test"
                Style="{StaticResource ActionButton}" />
        </StackPanel>

        <Grid Grid.Row="0" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Style="{StaticResource SeparatorRowStyle}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Table Columns Metadata" />
            <DataGrid
                Grid.Row="2"
                AutoGenerateColumns="True"
                ItemsSource="{Binding ColumnMetadataInfos}"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Grid>

        <Grid Grid.Row="2" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Style="{StaticResource SeparatorRowStyle}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Table Data" />
            <DataGrid
                Grid.Row="2"
                AutoGenerateColumns="True"
                ItemsSource="{Binding TableData, UpdateSourceTrigger=PropertyChanged}"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Grid>

        <!--  <StackPanel  -->
        <!--  HorizontalAlignment="Center"  -->
        <!--  Orientation="Vertical"  -->
        <!--  Visibility="Collapsed">  -->
        <!--  <Button  -->
        <!--  Command="{Binding ConfigTestCommand}"  -->
        <!--  Content="Read Config Test"  -->
        <!--  Style="{StaticResource ActionButton}" />  -->
        <!--  <Button  -->
        <!--  Command="{Binding ElevateDbTestCommand}"  -->
        <!--  Content="Elevate DB Test"  -->
        <!--  Style="{StaticResource ActionButton}" />  -->
        <!-- </StackPanel> -->
    </Grid>
</UserControl>
